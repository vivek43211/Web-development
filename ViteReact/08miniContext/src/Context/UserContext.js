
import React from 'react'

const UserContext = React.createContext()// it is method
// usercontext is provider 
// it wrap all the variable in it and act as global varible 
// with help of usercontext it can have all state access
// Context API is a state management solution in React that helps pass data between components in an organized way.

export default UserContext;